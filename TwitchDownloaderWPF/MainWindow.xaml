<Window x:Class="TwitchDownloaderWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TwitchDownloaderWPF"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture=""
        lex:ResxLocalizationProvider.DefaultAssembly="TwitchDownloaderWPF"
        lex:ResxLocalizationProvider.DefaultDictionary="Strings"
        mc:Ignorable="d"
        Title="KanekoLumi Twitch Downloader" MinHeight="440" Height="560"
        WindowState="Maximized"
        MinWidth="700" Width="800" Loaded="Window_Loaded" 
        SourceInitialized="Window_OnSourceInitialized">
    <Grid Background="{DynamicResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>

        <Frame Focusable="False" x:Name="Main" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0"
               NavigationUIVisibility="Hidden" Background="{DynamicResource AppBackground}" 
               BorderBrush="{DynamicResource AppBackground}"/>
    </Grid>
</Window>
